apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: keel-system
resources:
  - deployment.yaml
  - service.yaml
configMapGenerator:
  - name: prometheus
    files:
      - prometheus.yml
      - rules.yml
      - targets.json
